# Package configuration file for TangoSnmp installations
# It currently only imports the targets generated by cmake,
get_filename_component(TANGOSNMP_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
include("${TANGOSNMP_CMAKE_DIR}/TangoSnmpTargets.cmake")
